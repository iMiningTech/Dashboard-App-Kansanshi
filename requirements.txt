# pandas pinned <3.0 — the dashboard is written for pandas 2.x APIs; pandas 3.0
# introduced breaking changes (e.g. groupby on empty frames) that crash widgets.
pandas>=2.0.0,<3.0.0
numpy>=1.26,<2.3
pyyaml>=6.0
python-dateutil>=2.8.0
streamlit>=1.35.0
plotly>=5.20.0
boto3>=1.34.0
